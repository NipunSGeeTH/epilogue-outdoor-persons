/* Backend for the custom Epilogue form (NOT Google Forms). */

window.EPILOGUE_CONFIG = {
  /*
   * RECOMMENDED — works in incognito, keeps your custom form:
   *
   * 1. Open https://sheetdb.io → Sign in with Google
   * 2. Create API → pick your spreadsheet
   *    https://docs.google.com/spreadsheets/d/1FlWneIKvqvsh8XFS69vOB2968t9DEQeOLkqBZ_4_HXI
   * 3. Copy the API URL (https://sheetdb.io/api/v1/xxxxx)
   * 4. Paste it below
   * 5. In the sheet, row 1 headers must be exactly:
   *    Timestamp | Student Index | Guest Name | Guest NIC | Guest #
   */
  sheetdbUrl: "PASTE_SHEETDB_API_URL_HERE",
};
